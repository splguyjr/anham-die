import SwiftUI

// 플레이스홀더 — main 모듈이 Scribble 스타일 리스트로 구현 (PLAN §3.1)
struct MainWindowView: View {
    var body: some View {
        Text("메인 창 — 구현 예정")
            .frame(minWidth: 360, minHeight: 480)
    }
}
