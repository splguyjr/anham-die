import SwiftUI

// 플레이스홀더 — settings 모듈이 AppSettings 기반으로 구현 (PLAN §3.4)
struct SettingsView: View {
    var body: some View {
        Text("설정 — 구현 예정")
            .frame(width: 420, height: 320)
    }
}
